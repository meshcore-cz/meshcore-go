// Pure client-side SPA: no server-side rendering or prerendering. The Go backend
// serves the embedded shell and the browser does all data fetching.
export const ssr = false;
export const prerender = false;
